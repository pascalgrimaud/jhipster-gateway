machine:
    services:
        - docker
    java:
        version: oraclejdk8
    node:
        version: 4.5.0
dependencies:
    override:
        - npm install -g npm
        - npm install -g bower gulp
        - node -v
        - npm -v
        - bower -v
        - gulp -v
        - java -version
        - npm install
test:
    override:
        - ./mvnw clean
        - ./mvnw test
        - gulp test
        - ./mvnw package -Pprod -DskipTests
